// Taktflow Systems - Minimal logger component definitions
// Only defines GLOBAL — other components are provided by OpenBSW libraries

#include <util/logger/Logger.h>

DEFINE_LOGGER_COMPONENT(GLOBAL);
