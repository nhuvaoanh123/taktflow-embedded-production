FROM python:3.12-slim

RUN pip install --no-cache-dir python-can

RUN useradd -r -s /usr/sbin/nologin appuser

WORKDIR /app
COPY scripts/tcp_to_vcan_docker.py /app/relay.py

USER appuser
CMD ["python3", "/app/relay.py"]
